{{ define "base" }}
<!doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>{{ .Title }}</title>
  <style>
    body { font-family: system-ui, sans-serif; margin: 2rem; color: #FFF; background: #1f8dd6; }
    main { max-width: 40rem;display: block; margin: auto; }
  </style>
</head>
<body>
  <main>
    {{ template "content" . }}
  </main>
</body>
</html>
{{ end }}